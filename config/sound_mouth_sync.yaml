# sound_mouth_sync configuration
# Loaded via rosparam in sound_mouth_sync.launch

sound_mouth_sync:
  emotions:
    list:
      - neutral
      - happy
      - sad
      - angry
      - surprised
      - excited
      - sleepy
      - sleep
      - love
      - cute
      - confused
      - scared
      - bored
      - calm
      - disgusted
      - tired
      - cat
    default_emotion: neutral

  display:
    # emotion: mouth visible without /mouth/audio_wave; oscillogram needs audio_capture publishing
    start_display_mode: emotion
    default_emotion: neutral
    auto_mode: true                  # auto-switch oscillogram on sound, back to emotion on silence
    silence_return_sec: 3.0          # seconds of silence before returning to emotion mode
    # Idle sleep (mouth OLED): after no speech and no walking for idle_sleep_sec
    idle_sleep_enabled: true
    idle_sleep_sec: 120.0            # seconds of inactivity before idle overlay
    # While idle overlay is on, switch to another emotion from the pool every N seconds
    idle_sleep_rotate_sec: 30.0
    idle_sleep_emotions:
      - sleepy
      - cat
      - sleep
    idle_face_frame_ms: 120          # default frame duration for idle face PNG sequences (ms)
    fall_emotion: angry              # shown while /robot/posture is fall_*
    posture_topic: /robot/posture    # std_msgs/String from ainex_peripherals joystick_control
    movement_topic: /robot/is_moving # std_msgs/Bool — True while gait moving
    idle_require_movement_signal: false  # if true, idle-sleep waits for /robot/is_moving; false = works without joystick_controller
    audio_level_topic: /audio/level    # Float32 RMS 0..1 from audio_capture — wakes oscillogram if wave peaks are too low
    # Повторная отрисовка рта через N с после старта (0 = выкл). Имеет смысл 8–10 с, если после
    # включения на «рту» на мгновение виден мусор/чужой кадр; не устраняет дублирующий I2C-адрес.
    mouth_display_redraw_after_sec: 3.0
    # Повторно опубликовать /mouth/effective_* через N с (0 = выкл). Вместе с redraw помогает
    # «дожать» осциллограмму после старта, если подписчик подключился поздно.
    reassert_effective_topics_after_sec: 0

  audio_capture:
    rate: 48000                      # sample rate Hz (48000 = native USB card rate)
    chunk_size: 1024                 # samples per chunk
    wave_width: 128                  # oscillogram points (match display width)

  hardware:
    i2c_port: 1
    # Рот: SSD1306 должен быть на адресе 0x3D (перемычка ADDR на модуле). Системный OLED — 0x3C.
    # Два независимых экрана на одной шине требуют РАЗНЫХ адресов; при двух модулях на 0x3C
    # одна и та же картинка уйдёт на оба — это не исправляется только ROS-топиками.
    # После init_pose: опрос i2cdetect до появления 0x3D, не дольше N с (холодный старт).
    # Если рот «позже» встаёт на шину, увеличьте (например 12–15); при 0 — без ожидания.
    mouth_oled_startup_delay_sec: 7.0
    i2c_address: 0x3D
    width: 128
    height: 64
    rotate: 2                        # 0=normal, 2=180° (display mounted upside-down)
